## How to run

```bash
git clone git@github.com:elderapo/babel-7-property-decorator-issue.git
cd babel-7-property-decorator-issue
yarn install
yarn ts-node-start/babel-start # to see the difference in the output
yarn build # to see compiled code differences (babel vs tsc)
```
